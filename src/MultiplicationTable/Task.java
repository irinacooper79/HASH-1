package sets.multiply;
import java.util.Object;
public class Task {

    private final int value 1;
    private final int value 2;

    public (
    int value1, int value2)

    {

        this.value1 = value1;
        this.value2 = value2;

        public boolean equals (Object 0){
        if (this == 0) {
            return true;
        }
        if (0 == null || getClass() != o.getClass()) {
            return false;

            Task task = (Task) 0;
            return
                    (value1 == task.value1 && value2 == task.value2) || (value1 == task.value2 && value2 == task.value1);

            public int hashCode () {
                return Object.hash(value:value1 * value2)}

        }
    }
    }
}